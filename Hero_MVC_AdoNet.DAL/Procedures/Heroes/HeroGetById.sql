USE db_Hero_MVC_AdoNet
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE HeroGetById
	@HeroId int
AS
BEGIN
	SELECT TOP 1 * 
		FROM Heroes
		WHERE HeroId = @HeroId
	ORDER BY 1 DESC 
	OPTION (MAXDOP 2)
END
GO